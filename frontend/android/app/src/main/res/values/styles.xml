<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Thème de base de l'application -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Thème sans barre d'action (utilisé pendant le chargement web) -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!--
        Thème du Splash Screen KUKASOKO
        - Fond jaune via @color/kukasoko_yellow
        - Logo centré via @drawable/splash (layer-list XML)
        - Pas de rognage, adapté à tous les écrans
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:background">@color/kukasoko_yellow</item>
        <item name="windowSplashScreenBackground">@color/kukasoko_yellow</item>
    </style>

</resources>